info:
  name: list-messages-for-conversation
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/conversations/:conversationId/messages
  params:
    - name: conversationId
      value: "22"
      type: path
      description: The ID of the conversation
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
